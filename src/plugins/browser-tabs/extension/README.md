# Chromium extension (shipped with browser-tabs plugin)

Load this folder as an unpacked extension in Chrome / Edge.

Released path after Hiven starts:

```text
~/.local/hiven/plugins/builtin/browser-tabs/extension
```

Use **Plugins → Browser Tabs → Open extension folder** in Hiven settings.
